rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /healthData/{document} {
      // Allow read/write for all users (single-user personal app)
      allow read, write;
    }
  }
}